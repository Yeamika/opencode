Read a file or directory through RemoteExecutor. Defaults to the local executor; pass executor to read from another configured executor. If the path does not exist on the selected executor, an error is returned.

Usage:
- The filePath parameter should be an absolute path.
- By default, this tool returns up to 2000 lines from the start of the file.
- The offset parameter is the line number to start from (1-indexed).
- To read later sections, call this tool again with a larger offset.
- Use the rg tool to find specific content in large files or files with long lines.
- If you are unsure of the correct file path, use the glob tool to look up filenames by glob pattern.
- Contents are returned with each line prefixed by its line number as `<line>: <content>`. For example, if a file has contents "foo\n", you will receive "1: foo\n". For directories, entries are returned one per line (without line numbers) with a trailing `/` for subdirectories.
- Any line longer than 2000 characters is truncated.
- For binary files, use `mode: "binary"`. Binary mode uses byte offsets, returns a hex dump, and reads at most 128 bytes per call regardless of `limit`.
- If text mode detects binary content, the error includes the first binary byte offset and byte value, for example `offset 12: 0x00`.
- Call this tool in parallel when you know there are multiple files you want to read.
- Avoid tiny repeated slices (30 line chunks). If you need more context, read a larger window.
- Image files are read locally as file attachments and require executor=local. PDF reads are currently rejected.

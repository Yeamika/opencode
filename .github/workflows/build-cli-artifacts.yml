name: build-cli-artifacts

on:
  workflow_dispatch:
  push:
    branches:
      - "pr/**"
  pull_request:
    branches:
      - local-yes

permissions:
  contents: read

jobs:
  build-cli:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-tags: true

      - uses: ./.github/actions/setup-bun

      - name: Generate local package version
        run: |
          VERSION="0.0.0-local-yes-$(date -u +'%y%m%d%H%M')"
          echo "LOCAL_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "LOCAL_VERSION=$VERSION"

      - name: Build
        run: ./packages/opencode/script/build.ts
        env:
          OPENCODE_VERSION: ${{ env.LOCAL_VERSION }}
          OPENCODE_RELEASE: ""
          GH_REPO: ${{ github.repository }}
          GH_TOKEN: ${{ github.token }}

      - uses: actions/upload-artifact@v4
        with:
          name: opencode-cli
          path: |
            packages/opencode/dist/opencode-darwin*
            packages/opencode/dist/opencode-linux*
            packages/opencode/dist/opencode-windows*
